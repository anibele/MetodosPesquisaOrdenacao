package numeros;

import java.util.ArrayList;
import java.util.Random;

public class Util {
	public static void popularListaNumerosAleatorios(ArrayList<> lista, int quantidade, int inicio, int fim){
		Random gerador = new Random();
		int numero;
		for(; quantidade > 0; quantidade) {
			numero = 
			lista.add(numero);
		}
	}
}
